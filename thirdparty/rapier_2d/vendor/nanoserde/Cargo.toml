# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.81.0"
name = "nanoserde"
version = "0.2.1"
authors = [
    "makepad <info@makepad.nl>",
    "Fedor <not.fl3@gmail.com>",
]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = """
Serialization library with zero dependencies.
Supports Binary, JSON, RON and TOML."""
readme = "README.md"
license = "MIT OR Apache-2.0"
repository = "https://github.com/not-fl3/nanoserde"

[features]
binary = [
    "dep:nanoserde-derive",
    "nanoserde-derive/binary",
]
default = [
    "binary",
    "json",
    "ron",
    "toml",
    "std",
]
json = [
    "dep:nanoserde-derive",
    "nanoserde-derive/json",
]
ron = [
    "dep:nanoserde-derive",
    "nanoserde-derive/ron",
]
std = []
toml = []

[lib]
name = "nanoserde"
path = "src/lib.rs"

[[test]]
name = "bin"
path = "tests/bin.rs"

[[test]]
name = "json"
path = "tests/json.rs"

[[test]]
name = "parse"
path = "tests/parse.rs"

[[test]]
name = "ron"
path = "tests/ron.rs"

[[test]]
name = "ser_de"
path = "tests/ser_de.rs"

[[test]]
name = "toml"
path = "tests/toml.rs"

[dependencies.nanoserde-derive]
version = "=0.2.1"
optional = true
